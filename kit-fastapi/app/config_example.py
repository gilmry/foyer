"""Gabarit de configuration. En prod, tout vient de l'environnement (DB_HOST, DB_PORT, DB_NAME,
DB_USER, DB_PASSWORD ou DATABASE_URL). Aucun secret réel ici (gate plancher G1).
Copier/renseigner hors dépôt si besoin d'un fichier local (voir .gitignore)."""

# Exemple de variables (à exporter dans l'environnement, pas à committer avec des vraies valeurs) :
#   DB_HOST=localhost DB_PORT=5432 DB_NAME=todo DB_USER=todo DB_PASSWORD=change-me
